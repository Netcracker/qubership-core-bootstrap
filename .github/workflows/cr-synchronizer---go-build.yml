name: '[CR Synchronizer] Go build'

on:
  push:
    branches:
      - main
    paths:
      - 'cr-synchronizer/**'
  pull_request:
    types: [ opened, synchronize, reopened ]
    paths:
      - 'cr-synchronizer/**'

permissions:
  contents: read
  pull-requests: read
  packages: write

concurrency:
  group: ci-cr-synchronizer-build-${{ github.event_name }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  docker-build:
    if: ${{ !(github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork) }}
    uses: Netcracker/qubership-core-infra/.github/workflows/docker-build.yaml@8a233b03c5b5355d59c1585ffdaa1f45ea816405 # v2.6.1
    with:
      dry-run: false
      config-filename: "cr-synchronizer---docker-dev-config.json"
# Root SonarQube project configuration for multi-module project
sonar.projectKey=qubership-core-bootstrap
sonar.projectName=Qubership Core Bootstrap
sonar.projectVersion=1.0

# Define modules
sonar.modules=core-bootstrap,cr-synchronizer

# Root-level exclusions (applied to all modules unless overridden)
sonar.exclusions=**/node_modules/**,**/vendor/**,**/*.pb.go

# Module-specific configurations are in their respective sonar-project.properties files
# core-bootstrap/sonar-project.properties
# cr-synchronizer/sonar-project.properties

